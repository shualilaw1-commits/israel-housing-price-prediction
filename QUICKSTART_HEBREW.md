# 🚀 התחלה מהירה - 5 דקות

## מה יש לך כאן?

פרויקט **CrewAI** מלא עם:
- ✅ 6 סוכנים (agents) אוטונומיים
- ✅ 2 צוותים (crews)
- ✅ Flow אוטומטי
- ✅ 3 מודלי ML
- ✅ Dashboard אינטראקטיבי

## התקנה - 3 שלבים

### 1️⃣ צור סביבה וירטואלית

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Mac/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 2️⃣ התקן חבילות

```bash
pip install -r requirements.txt
```

⏱️ זמן: 2-3 דקות

🔑 **בונוס**: הקובץ `.env` כבר מוכן עם ה-API key שלך!
- ה-Agents ישתמשו ב-GPT-4 🤖
- קרא [API_SETUP.md](API_SETUP.md) לפרטים

### 3️⃣ הרץ!

```bash
python run.py
```

⏱️ זמן: 2-5 דקות

## מה קורה כשמריצים?

```
🏠 California Housing Price Prediction
├── 📊 צוות 1: מנתחי נתונים
│   ├── Agent 1: טוען נתונים
│   ├── Agent 2: מנקה נתונים
│   └── Agent 3: יוצר ויזואליזציות
├── ✅ Validation
└── 🤖 צוות 2: מדעני נתונים
    ├── Agent 4: מהנדס פיצ'רים
    ├── Agent 5: מאמן 3 מודלים
    └── Agent 6: מעריך מודלים
```

## התוצרים שתקבל

אחרי ההרצה תמצא ב-`outputs/`:

### נתונים 📊
- `clean_data.csv` - נתונים מנוקים
- `features.csv` - עם פיצ'רים חדשים

### מודלים 🤖
- `model.pkl` - המודל הטוב ביותר
- `all_models_comparison.json` - השוואה

### תיעוד 📝
- `insights.md` - תובנות מהנתונים
- `evaluation_report.md` - ביצועי המודל
- `model_card.md` - תיעוד מקצועי

### ויזואליזציות 📈
- `figures/` - גרפי EDA
- `evaluation_figures/` - גרפי הערכה

## Dashboard - ממשק אינטראקטיבי

```bash
streamlit run app/streamlit_app.py
```

נפתח ב: http://localhost:8501

### מה יש ב-Dashboard?

1. **🏠 עמוד הבית** - סקירה כללית
2. **📊 חקר נתונים** - גרפים אינטראקטיביים
3. **🤖 ביצועי מודל** - מטריקות והשוואות
4. **🎯 חיזוי מחיר** - נסה לחזות מחירים!

## פקודות שימושיות

```bash
# הרץ את כל הפרויקט
python run.py

# הרץ רק את ה-Dashboard
streamlit run app/streamlit_app.py

# בדוק התקנה
python -c "import crewai; print('✓ CrewAI installed')"

# ראה מבנה תיקיות
tree  # Mac/Linux
dir /s  # Windows
```

## בעיות? פתרונות מהירים

### ❌ pip install נכשל
```bash
pip install --no-cache-dir -r requirements.txt
```

### ❌ import errors
```bash
# ודא שהסביבה פעילה
# אמור לראות (venv) בתחילת השורה
```

### ❌ outputs ריקה
```bash
# הרץ שוב
python run.py
```

### ❌ Streamlit לא נפתח
```bash
# נסה פורט אחר
streamlit run app/streamlit_app.py --server.port 8502
```

## איך זה עובד?

### 1. Flow מחבר את הצוותים
```python
Flow:
  Start → Data Analysts → Validate → Data Scientists → End
```

### 2. כל צוות מריץ 3 agents
```
Data Analysts:      Data Scientists:
  1. Ingest           4. Engineer Features
  2. Clean            5. Train Models
  3. Analyze          6. Evaluate
```

### 3. Validation בין הצוותים
```
✅ בדיקת נתונים מנוקים
✅ בדיקת dataset contract
✅ וולידציה לפני המשך
```

## מה הלאה?

### רמה 1: סייר בתוצאות
- פתח קבצים ב-`outputs/`
- קרא את `insights.md`
- ראה גרפים ב-`figures/`

### רמה 2: שחק עם ה-Dashboard
- חקור נתונים
- ראה ביצועי מודל
- עשה חיזויים

### רמה 3: התאם אישית
- ערוך agents ב-`crews/*/agents.py`
- שנה משימות ב-`crews/*/tasks.py`
- הוסף tools ב-`crews/*/tools.py`

## מבנה הפרויקט

```
house-price-crewai/
├── crews/              # 2 צוותים
│   ├── data_analyst_crew/
│   └── data_scientist_crew/
├── flow/              # ה-Flow המחבר
├── app/               # Streamlit dashboard
├── outputs/           # כל התוצרים
├── run.py            # הרצה ראשית
└── README.md         # תיעוד
```

## טיפים

💡 **הרץ run.py לפני Streamlit** - צריך את התוצרים תחילה

💡 **בדוק outputs/** - שם הכל נשמר

💡 **קרא את הלוגים** - הם מסבירים מה קורה

💡 **שחק עם ה-Dashboard** - זה הכיף!

## עזרה מהירה

| בעיה | פתרון |
|------|--------|
| חבילות חסרות | `pip install -r requirements.txt` |
| outputs ריקה | הרץ `python run.py` |
| שגיאת import | בדוק ש-(venv) פעיל |
| Streamlit לא עובד | נסה פורט אחר |

## סיכום - 3 פקודות בלבד!

```bash
# 1. התקן
pip install -r requirements.txt

# 2. הרץ
python run.py

# 3. תיהנה
streamlit run app/streamlit_app.py
```

---

**זהו! עכשיו יש לך פרויקט CrewAI מלא ועובד! 🎉**

**שאלות?** תסתכל ב-README.md או SETUP.md
